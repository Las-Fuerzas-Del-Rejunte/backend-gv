import { LinesRepository } from 'src/modules/lines/repositories/lines.repository';

describe('LinesRepository', () => {
  it('should be defined', () => {
    expect(LinesRepository).toBeDefined();
  });
});
