import { SalesRepository } from '../src/modules/sales/repositories/sales.repository';

describe('SalesRepository', () => {
  it('should be defined', () => {
    expect(SalesRepository).toBeDefined();
  });
});
