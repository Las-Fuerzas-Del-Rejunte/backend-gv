import { SupabaseCrudRepository } from '../src/common/repositories/supabase-crud.repository';

describe('SupabaseCrudRepository', () => {
  it('should be defined', () => {
    expect(SupabaseCrudRepository).toBeDefined();
  });
});
